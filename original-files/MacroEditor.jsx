// components/MacroEditor.jsx
import React from 'react';

export default function MacroEditor({ device }) {
  return (
    <div className="macro-editor">
      <h3>MACRO EDITOR</h3>
      <p style={{ color: 'var(--text-secondary)' }}>
        Macro support coming soon.
        <br />
        Uses VIA's dynamic macro commands (not yet wired up).
      </p>
    </div>
  );
}
